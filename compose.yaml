services:
  ashfur:
    image: ghcr.io/asoji/ashfur:latest
    container_name: ashfur_bot
    env_file: .env
    depends_on:
      - mongo

  mongo:
    image: mongo
    container_name: ashfur_mongo
    restart: always

networks:
  default:
    name: ashfur_network